HTTP 메서드에는 GET,POST,PUT,PATCH,DELETE,HEAD,OPTION,CONNECT,TRACE 가 있다.
  GET은 리소스를 조회할때 서버에게 ~좀 주세요 라고 요청한다
  서버에 전달하고  싶은 데이터는 query를 통해서 전달한다 메세지 바디를 사용할 수 있지만 권장하지 않는다.
  
  POST가 하는 일은 거의 모든 것을 할 수 있다.
  POST가 하는 일이 많아서 정리하자면 이 리소스 RUI에 POST 요청이 오면 요청 데이터를 어떻게 처리할지 리소스마다 따로 정해야 한다
  -> POST는 딱 정해진 것이 없다.
  주로 '조회'하는 역할을 하지만 
  새로운 리소스를 등록하거나 
  요청데이터를 처리하거나
  다른 메서드로는 처리하기 애매할 때 사용한다
  
  PUT은 리소스를 대체할때 사용한다.
  완전히 대체하기때문에 수정이아닌 갈아 엎는다고 생각하면된다.
  ex) 폴더에 덮어쓰기 생각하면 쉽다.
  
  PATCH가 위에서 하지못한 수정의 역할을 한다.
  리소스 부분 변경
  
  DELETE 리소스 제거
  
  
HTTP 메소드의 속성에는 안전, 멱등, 캐시가능 이 있다.

  안전은 호출해도 리소스를 변경하지 않는지
  
  멱등을 같은 호출을 반복해도 결과가 같은지
  POST는 멱등이 아니다
  캐시가능은 응답 결과를 캐시해도 되는지
  GET,HEAD,POST,PATCH가 캐시 가능이지만 실제로는 GET,HEAD 정도만 캐시사용한다
